language: python
python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5
    - 3.5-dev
    - 3.6
    - 3.6-dev
install:
    - pip install -r requirements.txt
    - pip install -r requirements-test.txt
script:
    - nosetests --with-coverage --cover-package=alfredo
after_success:
    - coveralls
